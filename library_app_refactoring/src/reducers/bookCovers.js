import {SET_BOOKS_COVERS} from "../constants/actionTypes";

  const bookCovers = (state = {}, action) => {
    switch (action.type) {
      default:
        return state;
    }
  }

export default bookCovers;
